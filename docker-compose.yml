services:
  web:
    build: .
    container_name: mango-fastapi

    env_file:
      - .env

    ports:
      - "8003:8000"

    volumes:
      - .:/app

    command: uvicorn main:app --host 0.0.0.0 --port 8000

    restart: always